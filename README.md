# React-project
# Docker 
# install docker 
https://www.docker.com/
* build *
  =
  docker compose build
* run *
  =
  docker compose up -d
# react running on
http://localhost:3000/
# server running on 
http://localhost:5000/
